import Button from '../button/Button';

const Operators = (props) => {

    return(
        <div>
            <Button name="+"></Button>
            <Button name="-"></Button>
            <Button name="*"></Button>
            <Button name="/"></Button>
            <Button name="="></Button>
        </div>
    );
}

export default Operators;